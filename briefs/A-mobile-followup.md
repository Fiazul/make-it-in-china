Your sandbox cannot bind ports or launch Chromium; the orchestrator will run the verification for you. Change of plan, same goal (a phone player can fully control and play the game):

1. Do the full audit and implement every fix now, without live verification: read `src/render/input.ts`, `src/render/scene.ts`, `src/render/camera.ts`, `src/style.css` (touch-controls block ~168–230, phone sheet ~1315–1345), `index.html`, `src/ui/shell.ts`, `src/ui/bubble.ts`, `src/audio/manager.ts`, `src/main.ts`. Reason carefully about Chrome-on-Android behaviour: pointer capture, per-pointerId multi-touch, `touch-action`, `preventDefault` on touch-derived pointer events, `pointercancel`, media queries for showing controls (`pointer: coarse` / `maxTouchPoints`, not width alone), z-index vs canvas and modals, `pointer-events` on the container vs children, viewport meta (`viewport-fit=cover`, `interactive-widget=resizes-content`), `dvh`/safe-area, audio unlock on any first touch on any element, reply tap-to-preview/tap-to-select, DPR/quality path on coarse pointer.
2. Finish `tests/e2e/mobile-touch.mjs` as a standalone Node script using `playwright` (already a dependency? if not, use `import { chromium, devices } from 'playwright'` and note it as a devDependency to add) that takes `BASE_URL` from env (default http://localhost:4174), emulates `devices['Pixel 7']` with `hasTouch`, and performs the checks from the original brief with real touch dispatch (`page.touchscreen.tap`, CDP `Input.dispatchTouchEvent` for drags), printing PASS/FAIL per check and saving screenshots to `docs/screenshots/mobile-*.png`. Add `npm run e2e:mobile`.
3. Run `npx tsc --noEmit`, `npm test`, `npm run build` (these work in your sandbox).
Report: root causes (file:line) / changes / gate output / exact command for the orchestrator to run the touch script.
