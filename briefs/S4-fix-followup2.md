All 25 curriculum scenes are authored. Strict rule 4 PASSES, but the legacy `npm run check:content` rule 4 (inline in `scripts/check-level.mjs`) now FAILS: "7/150 phase-list words appear in fewer than 3 non-consequence scenes", because it excludes every `kind: consequence` scene while the design counts the guided consequence S11 (`p1_wrong_bus_01`, which has a `curriculumIndex`) as curriculum. Align legacy rule 4 to count scenes with a defined `curriculumIndex` (falling back to kind≠consequence when no scene has curriculumIndex, so v1 fixtures behave as before), keep its WARN/FAIL threshold logic, and add a test in `tests/check-level-strict.test.ts` or a new legacy test proving a curriculum-indexed consequence scene counts. CI runs the legacy command, so this must PASS on current content. No shell; do not touch scenes.json. Report DONE / FILES.
