Gates: tsc clean after a one-line narrowing fix in notebook.ts (kept). One of your tests fails:
`tests/audio/manager.test.ts > recovers a stored slotted sentence by its rendered text` — expected 'base__v000', received 'base' (manager.sentence('base', '两个杯子。') does not find the variant by rendered text). Decide whether the implementation or the test is wrong against the manifest shape (`variants[base].byBinding` keys are `slot=valueId|…`; clips carry `text`), fix it so the notebook's first-seen sentence resolves to the correct variant clip, and keep the test meaningful. No shell; report DONE / FILES.
