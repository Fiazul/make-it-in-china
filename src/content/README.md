# Content types

Shared TypeScript contracts for authored JSON loaded by the game and preview. Not the JSON data itself (see `content/`).

| File | Role |
| --- | --- |
| `types.ts` | Word, scene, world, slot, voice, and ambient shapes |
